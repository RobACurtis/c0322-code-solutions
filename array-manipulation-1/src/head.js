/* exported head */
// define a function
// select the first index of the array
// assign it to a new var
// return the var from the function
function head(array) {
  var index = array[0];
  return index;
}
