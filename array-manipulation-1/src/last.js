/* exported last */
// define a function
// select the last index of the array
// assign it to a new var
// return the var
function last(array) {
  var last = array[array.length - 1];
  return last;
}
