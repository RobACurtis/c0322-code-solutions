/* exported isUpperCased */
// define a function
// check if the word is equal to the word uppercased.
// if its true return true
// else return false

function isUpperCased(word) {
  if (word === word.toUpperCase()) {
    return true;
  } else {
    return false;
  }
}
