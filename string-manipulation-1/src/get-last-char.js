/* exported getLastChar */
// define the function to find the last Character
// create a variable for the string
// create a variable for the last index number length -1
// return the character at the last index
function getLastChar(string) {
  var str = string;
  var index = str.length - 1;
  return str.charAt(index);
}
