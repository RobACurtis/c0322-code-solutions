/* exported getFirstChar */
// function definition
// create a variable with the value of the string
// create a variable with the index 0
// return the index using charAt
function getFirstChar(string) {
  var str = string;
  var index = 0;
  return str.charAt(index);
}
